import { HomeClient } from "@/components/site/home-client";

export const metadata = {
  title: "Components — DevKit",
  description: "Browse reusable UI components for React and Angular. Copy-paste ready.",
};

export default function ComponentsPage() {
  return <HomeClient />;
}
